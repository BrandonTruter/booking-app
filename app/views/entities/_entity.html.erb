<tr>
  <td><%= entity.title %></td>
  <td><%= entity.description %></td>
  <td><%= link_to "Diary", diaries_path(entity_id: entity.id), title: 'Open the diary', class: 'btn btn-primary' %></td>
</tr>
