<div>
  <h1 class="font-bold text-4xl text-blue-500">Bookings</h1>
  <table>
    <thead>
      <tr>
        <th>State</th>
        <th>Reason</th>
        <th>Schedule</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <%= render partial: "booking", collection: @bookings %>
    </tbody>
    <tfoot>
      <%= link_to 'New booking appointment', new_booking_path, class: 'btn btn-primary' %>
    </tfoot>
  </table>
</div>
