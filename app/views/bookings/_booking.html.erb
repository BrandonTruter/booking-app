<%= turbo_frame_tag "booking_#{booking.id}" do %>
  <div style='border-style: ridge; padding: 20px;' class='mt-2'>
    <div class='mt-2 d-flex'>
      <%= link_to "Edit", edit_booking_path(booking), style: 'margin-left: 5px' %>
      <%= link_to "Delete", booking_path(booking), style: 'margin-left: 5px', data: {confirm: "Are you sure?", "turbo-method": :delete} %>
      <%#= link_to "Diary", diary_path(booking&.diary) %>
    </div>
  </div>
<% end %>
