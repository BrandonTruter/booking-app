<div class="flex justify-center border-b border-gray-900/10 pb-12">
  <h1 class="text-base font-semibold text-blue-500">Edit Booking</h1>

  <div class="flex flex-col">
    <%= turbo_frame_tag "booking_#{@booking.id}" do %>
      <%= render partial: 'form', object: @booking %>
    <% end %>
  </div>
</div>
